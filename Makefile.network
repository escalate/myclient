.DEFAULT_GOAL := connections

.PHONY: install
install:
	sudo apt-get install --yes network-manager openvpn network-manager-openvpn vpnc network-manager-vpnc resolvconf

.PHONY: connections
connections: install
	sudo cp --force $(HOME)/secrets/.network/* /etc/NetworkManager/system-connections/
